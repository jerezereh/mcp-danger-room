# Commits that changed formatting and nothing else.
#
# GitHub honours this file automatically in its blame view. Locally:
#
#     git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# Only add a commit here if it is *provably* behaviour-preserving — a
# whole-repo formatter run, a rename with no logic change. A commit that
# sneaks a fix into a reformat and lands here becomes invisible to blame,
# which is worse than never having the file.

# Reformat with Prettier, and nothing else (#31)
a49a164f8f629718315ee5b5264f45599e65a066
